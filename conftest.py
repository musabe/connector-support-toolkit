# conftest.py
#
# connector_toolkit is installed as an editable package via:
#   pip install -e .
#
# That puts src/ on sys.path automatically, so no path manipulation is needed
# here. If you are running tests WITHOUT installing the package first, run:
#   pip install -e ".[dev]"
